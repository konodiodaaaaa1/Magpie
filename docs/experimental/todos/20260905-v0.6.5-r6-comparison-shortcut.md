# r6 local 对比快捷键更新

用户要求将工具栏“原图对比”改为“对比”、去掉说明，并增加快捷键设置，覆盖部署到 r6 local。

- 工具栏悬停只保留“对比”名称；英文 Compare，繁体對比。删除该悬停说明资源，同步状态名称。
- 在现有“工具栏快捷键设置”折叠项内加入“对比”，使用与工具栏一致的图标及现有 ShortcutControl。
- 默认快捷键 Alt + Shift + C；可编辑、检查冲突并保存为 `shortcuts.comparison`。旧配置缺少该项时补默认值，已有其他快捷键不改写。
- 新动作追加在枚举末尾，不改变既有动作编号；沿用现有注册、键盘钩子、防连发及运行会话校验。
- 快捷键调用与按钮相同的对比状态切换，并触发稳定图像重绘；保持效果链、补帧及会话设置。
- 继续使用 `0.6.5-r6-local` 版本和既有目录／ZIP。用户已确认从托盘退出，部署过程不强制结束进程。

## 完成结果

以下保留快捷键更新时的部署记录。后续角标更新已覆盖同一目录，最新产物哈希见 [对比角标更新](20260905-v0.6.5-r6-comparison-badge.md)。

- Release x64 完整构建成功，退出码 0，无编译警告或错误。版本仍为 `0.6.5-r6-local`。
- 三种语言本次 15 个新增／修改文案均匹配实际 PRI；三份悬停说明资源已删除。快捷键设置 XAML、资源键、配置读写与动作分发链已核对。
- 已覆盖 `release/v0.6.5-r6-local/Magpie-v0.6.5-r6-local-x64` 和同名 ZIP。全部 235 个文件与 ZIP 条目通过 manifest／SHA256 核对，170 个效果器文件与源码一致。
- ZIP：487,060,241 字节；SHA256 `2A9A5A4406CAFC65B77B3B0CE34A265588FE9BF429B42C2879BD4F7BA544F8A7`。
- EXE SHA256：`30859F5103A0B79A9717FBBC9B7E26481310DB67D7F1EF9C2C951CA2F153C21E`。
- PRI SHA256：`C1BE047453717B811E60C807FEF0D83041814D4C96D0A7D2FD180AB3FB81CCD6`。

构建日志：`source/build-r6-compare-shortcut-console.log`；打包日志：`source/build-r6-compare-shortcut-package.log`；校验报告与修改前副本：`.tools/r6-compare-shortcut`。

实际操作验收由用户负责，未启动 Magpie，未使用 computer-use。
