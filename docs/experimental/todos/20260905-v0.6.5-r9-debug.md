# 0.6.5 r9 debug 交付记录

日期：2026-09-05。目的：调查 Endfield／视频中可感知的卡顿和低 1% Low，保留 r9 行为作为诊断基线。

## 已完成

- 创建 Git 分支 `065-r9-debug`。基于当前 HEAD `84d9f6abb8203a79f9a44d9a66984a293e68856d`，携带原工作区未提交的 r1–r9 改动；没有把这些改动误记为新的干净提交，也没有提交本次改动。
- 新增 `FrameTrace.h/.cpp`，记录捕获、效果处理、共享纹理发布、前端输入及呈现等 CPU 时间线。前后端独立定长内存环，运行期不逐帧写文件；停止效果、后端退出后导出 CSV。
- 新增 Python 离线分析器与 `docs/FRAME_TRACE_GUIDE.md`，部署包包含 `Diagnostics/Analyze-FrameTrace.py` 和说明。
- `/p:EnableFrameTrace=true` 专用于本诊断构建；正式构建默认关闭。`MAGPIE_FRAME_TRACE=0` 可关闭诊断版新增采集，以便用户做对照。
- 保持 Release x64 优化构建，版本 `0.6.5-r9-debug-local`。未执行此前性能 review 中的渲染清理候选，避免混淆基线。

## 编译与校验

用户允许提高编译并发。读取 CPU 为 Ryzen 9 9950X3D，32 逻辑处理器；本地 `.tools/r9-debug/build.ps1` 使用 MSBuild `/m:4`，`UseMultiToolTask=true`、`CL_MPCount=16`、`MultiProcMaxCount=16`、`EnforceProcessCountAcrossBuilds=true`，限制为跨构建共享的最多 16 个编译任务。

Release x64 编译成功。离线分析器 6 项合成检查通过，覆盖完整性、慢事件副本、不同交换链、非有限数值及指标语义。用户随后运行诊断版，成功导出 44.126 秒完整记录，真实写出格式也通过分析器校验。

没有由助手启动 Magpie、进行 computer-use 或 GPU／画面验收；实际效果和性能测试由用户进行。

## 部署

独立目录：`release/v0.6.5-r9-debug-local/Magpie-v0.6.5-r9-debug-local-x64`。

压缩包：`release/v0.6.5-r9-debug-local/Magpie-v0.6.5-r9-debug-local-x64.zip`，487086571 字节。共 228 个发布文件（含清单），其中 161 个效果器 HLSL；清单和 ZIP 各条目哈希验证通过，运行生成的日志和缓存不纳入压缩包。原 r9 local 保留。

| 文件 | SHA-256 |
| --- | --- |
| ZIP | `D47FAAA77094E0FEDD881989648544AFEDC434E50518F4BB643EE62D44253BBF` |
| Magpie.exe | `68D272EFDBE80C7E041432754C4A1D64C53868ABF6821C5E95645FC9B9229B4B` |
| resources.pri | `9DF47F199728ED4DC115F7884D9A5EA6084EBD081AEF83EA6E0A9861CCDBE1A3` |

构建记录与校验结果保存在工作区 `.tools/r9-debug/`。发布清单明确记录 `sourceDirty=true`。

## 首轮调查

详见 `docs/experimental/reviews/20260905-v0.6.5-r9-debug-first-trace-REVIEW.md`。已定位两次捕获输入间隔增大和一次 NR 调用尖峰，尚未确认整个 1% Low 问题的根因；下一步证据应覆盖捕获通知／跳帧和实际显示。
