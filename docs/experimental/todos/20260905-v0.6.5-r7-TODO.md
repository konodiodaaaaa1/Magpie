# 0.6.5 r7 local 实施记录

日期：2026-09-05。状态：实现、编译、本地部署与产物校验完成；实机验收由用户进行。

后续已覆盖部署性能分析器拖动修复，当前 r7 local 的最新产物及哈希以 [r7 fix 实施记录](20260905-v0.6.5-r7-fix-TODO.md) 为准。下文保留 r7 初始交付记录。

## 范围与完成项

- [x] 清点 SR、NR、FG、诊断等外部光流消费者，并核对已有共享行为。
- [x] 按最终确认规则统一选择一个提供者：NVOF 优先，其次 AMD，再内置；选定来源只取实际申请中的较高档位，不提升到未申请的最高档。
- [x] 每捕获帧估算一次；所有启用光流的消费者映射到选中结果和尺寸适配缓存，关闭光流的保持零向量。跨尺寸适配等待提供者同步。
- [x] 旧版 FSR2、FSR3／4、XeSS SR 的 HalfResOpticalFlow 接入统一池，基于捕获原图，保留算法。
- [x] 来源／档位申请不一致时列出相关效果器与目前统一使用的方法和档位，使用非报错 toast，并保留在运行参数页。不改写保存的申请值，不提示分别估算。
- [x] 依据 Intel、NVIDIA、Microsoft 一手文档审查 FG 队列、资源生命周期与帧率语义。
- [x] 修复 XeSS 零向量模式重复历史重置、帧 ID、丢帧历史、Present 周期与输入复用同步。
- [x] DLSS 每次读取禁止插值输出，跳过重置／禁用结果；防止旧队列消息干扰新槽位。
- [x] DLSS 按捕获周期计算逐帧呈现间隔，间隔随槽位传递；长卡顿不连续追赶过期时间点。
- [x] 同一捕获帧的参数重绘不重复生成；长暂停、焦点／捕获中断重置 DLSS 历史。
- [x] 工具栏与性能页在 FG 存在时显示总／真实 FPS；按实际提交或 SDK 计数，未知值不虚构。
- [x] 完成 Release x64 编译、资源检查、纯逻辑检查、打包及全文件校验。

研究、消费者矩阵、实现边界和用户验收项目见 [完整 review](../reviews/20260905-v0.6.5-r7-motion-sharing-fg-REVIEW.md)。简要发布说明见 [r7 release notes](../../RELEASE_NOTES_v0.6.5-r7-local.md)。HDR 继续留到后续版本。

## 验证结果

- 完整 Release x64 构建通过；第一次构建存在第三方 FSR DX12 后端 C4324 对齐警告。最终统一光流选择、焦点历史和错误日志修正均重新编译通过，退出码 0，最终控制台日志未报告编译错误或警告。
- EXE FileVersion／ProductVersion 均为 `0.6.5-r7-local`。
- 独立 C++20 检查使用 `/W4 /WX` 编译通过：全部 128 种请求子集的选择来自实际申请、NVOF 优先、单实例、合并顺序无关、消费者共享及关闭保留零向量、非法质量忽略；120/60、部分跳过、未知与空闲计数；捕获周期、限帧间隔、长暂停和卡顿后截止时间。
- 所有 RESW XML 可解析，资源键无重复；简体、繁体、英文共 27 个新增文案逐项匹配编译后的 PRI。
- 170 个随包 HLSL 文件与源码逐字节一致。
- 部署目录与 ZIP 的全部 235 个文件通过 manifest、大小和 SHA256 校验；EXE、PRI、Updater、TouchHelper 与最终构建输出一致。未包含用户配置、cache、logs 或调试符号。
- 用户确认退出，进程检查未发现 Magpie 后，使用 `-RequireMagpieClosed` 打包到新 r7 目录；没有覆盖 r6，没有启动 Magpie，没有使用 computer-use。
- 用户随后确认统一来源／申请档位选择规则，已重新编译并覆盖同一 r7 local 目录及 ZIP；再次检查进程已退出，完整产物校验通过。下方哈希为该最终修订版。

纯逻辑检查不覆盖 GPU 命令执行、画质、驱动并存限制或物理扫描帧间隔。新 FPS 表示提交的内容帧率，不能当成显示扫描测量；实机验收项目列在 review 尾部。

## 产物

- 程序：`release/v0.6.5-r7-local/Magpie-v0.6.5-r7-local-x64/Magpie.exe`
- ZIP：`release/v0.6.5-r7-local/Magpie-v0.6.5-r7-local-x64.zip`
- ZIP 大小：487,068,590 字节。
- ZIP SHA256：`791508BE46E7B1506F526AA582E9D1C076F929CF5D4BB0A73986B1C48DF2BC32`
- EXE SHA256：`5E4CFB3BC1EC9169690BDDA70F5CDFF5176C3DC03BD73B920C9492B06B9A420F`
- PRI SHA256：`E130F32F23054E41CA0489DE4AA44E0A46E0E806F06AA1ECF674359A8337CE33`

以当前未提交工作树构建，基准提交 `84d9f6abb8203a79f9a44d9a66984a293e68856d`；不代表工作树内容已提交到该 commit。本轮没有提交、创建 tag 或推送。

构建日志：`source/build-r7-console.log`、`source/build-r7.log`；打包日志：`source/build-r7-package.log`。本轮开始前源码副本、相对于 r6 的修改补丁、检查程序、PRI 转储及产物校验 JSON 均保留在 `.tools/r7`。

## 后续操作偏好

用户已明确授权：以后部署遇到正在运行的 Magpie，可以协助正常退出，不必每次等待用户手动退出；正常退出完成并确认进程结束后再部署。本次用户已自行退出，没有调用结束进程操作。实机运行测试仍由用户自行完成。
